import os

BOT_TOKEN = os.getenv("BOT_TOKEN")

OWNER_ID = 1055122209

ALLOWED_USERS = [
    OWNER_ID
]

AUTO_SIGNAL_SCORE = 85
AUTO_SCAN_INTERVAL_MINUTES = 5
AUTO_SIGNAL_COOLDOWN_MINUTES = 120

DEFAULT_TIMEFRAMES = {
    "main_trend": "4h",
    "mid_trend": "1h",
    "structure": "30m",
    "entry_1": "15m",
    "entry_2": "5m"
}

MAX_LEVERAGE_SUGGESTION = 5
RISK_PER_TRADE_PERCENT = 1

# تنظیمات قابلیت‌های جدید
NEWS_FILTER_ENABLED = os.getenv("NEWS_FILTER_ENABLED", "0") == "1"
HIGH_IMPACT_NEWS = os.getenv("HIGH_IMPACT_NEWS", "0") == "1"
NEWS_BLOCK_MINUTES_BEFORE = int(os.getenv("NEWS_BLOCK_MINUTES_BEFORE", "30"))
NEWS_BLOCK_MINUTES_AFTER = int(os.getenv("NEWS_BLOCK_MINUTES_AFTER", "30"))

VERY_SAFE_MIN_SCORE = int(os.getenv("VERY_SAFE_MIN_SCORE", "88"))
VERY_SAFE_MIN_WIN_PROBABILITY = int(os.getenv("VERY_SAFE_MIN_WIN_PROBABILITY", "75"))

TRACKER_WARNING_ENABLED = os.getenv("TRACKER_WARNING_ENABLED", "1") == "1"
TRACKER_CHECK_INTERVAL_SECONDS = int(os.getenv("TRACKER_CHECK_INTERVAL_SECONDS", "60"))
