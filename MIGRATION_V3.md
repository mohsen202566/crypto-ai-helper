# مهاجرت به استراتژی V3 — Extreme Pump Fade

## خلاصه
استراتژی قبلی (Momentum Ignition: دنبال‌کردن پامپ با لانگ/شورت) کامل حذف شد
و جای آن استراتژی V3 نشست: **فقط شورت، بعد از پامپ افراطی**.

قوانین V3 **FROZEN** هستند. هیچ پارامتری نباید در طول Paper Test تغییر کند.

---

## جریان کار

```
24h Change >= 15%  ->  WATCHLIST  (اسکن هر 15 دقیقه)
                           |
                    MONITORING  (هر 5 دقیقه)
                           |
          +----------------+----------------+
          |                                 |
   NORMAL_REVERSAL                   FAST_CASCADE
   خستگی (wick >= 0.5)              شتاب‌گیری نزولی
   کاهش شتاب                        + دامنه >= 2x
   شکست کف تأییدشده                 + حجم >= 2x
          |                                 |
          +----------------+----------------+
                           |
                        SHORT
                           |
         حد ضرر = سقف تأییدشده + 1.5 x ATR(14)
         (ATR در لحظهٔ ورود فریز می‌شود)
                           |
              HOLD — نویز خروج ایجاد نمی‌کند
                           |
        سقف بالاتر تأییدشده + کف بالاتر تأییدشده
                           |
                         EXIT
                           |
                  استراحت (پیش‌فرض 2 ساعت)
```

---

## پارامترهای قفل‌شده (`config.py`)

| پارامتر | مقدار | توضیح |
|---|---|---|
| `WATCHLIST_MIN_GAIN_PCT` | 15.0 | آستانهٔ کاندید شدن |
| `WICK_RATIO_MIN` | 0.5 | نسبت سایهٔ بالایی برای خستگی |
| `DECEL_WINDOW_BARS` | 3 | پنجرهٔ مقایسهٔ شتاب |
| `PIVOT_CONFIRM_BARS` | 2 | تأیید Pivot از هر طرف |
| `CASCADE_EXPANSION_MULT` | 2.0 | ضریب انبساط دامنه و حجم |
| `CASCADE_BASELINE_BARS` | 12 | پنجرهٔ پایه (۱ ساعت) |
| `CASCADE_MIN_ELAPSED_SECONDS` | 60 | حداقل زمان کندل جاری |
| `ATR_PERIOD` | 14 | دورهٔ ATR |
| `STOP_ATR_MULT` | 1.5 | بافر نوسانی حد ضرر |

**این اعداد تغییر نمی‌کنند.** اگر نتیجه بد بود، دلیلش ثبت می‌شود
(تأخیر ورود، استاپ بزرگ، تشخیص دیر آبشار، ...) — ولی پارامتر عوض نمی‌شود.
این همان دام Overfitting است که باید از آن دور بمانیم.

---

## تنظیمات پنل (قابل تغییر توسط شما)

| دستور | اثر |
|---|---|
| `دلار ۱۰` | مارجین هر پوزیشن |
| `پوزیشن ۵` | حداکثر پوزیشن هم‌زمان |
| `اهرم ۱۰` | لوریج |
| **`استراحت ۲`** | **ساعت استراحت هر نماد بعد از خروج (۱ تا ۱۲)** |
| `سقف ۵۰` | سقف سرمایهٔ درگیر |
| `گزارش ۱۵` | فاصلهٔ گزارش خودکار |

---

## دستورات جدید

| دستور | نتیجه |
|---|---|
| `واچ` | نمادهای زیر نظر با پامپ فعلی و اوج ثبت‌شده |
| `قیف` | از چند کاندید، چند ستاپ و چند معامله بیرون آمد |
| `چرا` | گزارش آخرین مانیتور + دلیل دقیق رد شدن هر نماد |

**حذف‌شده:** `پامپ N`، `ضریب حجم N`، `تریل N` — این‌ها بخشی از قوانین
قفل‌شده‌اند و دیگر از پنل قابل تغییر نیستند.

---

## داده‌های ثبت‌شده برای تحلیل بعدی

سه جدول جدید در دیتابیس:

- **`watchlist`** — هر کاندید با زمان ورود، اوج پامپ، تعداد ستاپ و ترید
- **`signal_snapshots`** — عکس کامل شرایط در لحظهٔ هر تریگر (ورود یا رد)
  شامل: wick، نرخ شتاب، دامنه، حجم، swing high/low، ATR، فاصلهٔ استاپ
- **`cooldowns`** — تاریخچهٔ استراحت هر نماد

برای هر معامله‌ی بسته‌شده در `events` ثبت می‌شود: دلیل خروج، جزئیات،
قیمت ورود/خروج، حد ضرر، MFE، مدت معامله، ساعت استراحت.

**نکته:** MFE/MAE فقط برای تحلیل بعد از معامله‌اند و هیچ نقشی در تصمیم
ورود یا خروج ندارند.

---

## تضمین‌های ساختاری

- **بدون Look-Ahead:** Pivot فقط با تأیید ۲ کندل شناخته می‌شود (۱۰ دقیقه
  تأخیر عمدی). ارزیابی ورود intra-candle است ولی فقط از قیمت همان لحظه
  استفاده می‌کند. baseline همیشه از کندل‌های بسته‌شده محاسبه می‌شود.
- **بدون Fallback مبهم:** اگر دادهٔ حجم معتبر نباشد، FAST_CASCADE اصلاً
  مجاز نیست (تعریف Setup در لحظه عوض نمی‌شود).
- **حد ضرر سخت مطلق:** به محض فعال شدن، فوراً بسته می‌شود؛ منتظر هیچ
  تأیید تکنیکالی نمی‌ماند.
- **ATR فریز:** بعد از ورود، حد ضرر با ATR جدید عقب نمی‌رود.
- **NO TRADE معتبر است:** اگر استاپ ساختاری با مارجین موجود منطقی نباشد،
  معامله انجام نمی‌شود و دلیلش ثبت می‌شود.

---

## قبل از اجرا

```bash
# 1) پشتیبان از دیتابیس فعلی (آمار استراتژی قبلی)
cp runtime.db runtime_momentum_backup.db

# 2) اجرا — جداول جدید خودکار ساخته می‌شوند
systemctl restart staged-bot

# 3) در تلگرام
راهنما          # دیدن دستورات جدید
استراحت ۲       # تنظیم مدت استراحت
ترید مجازی فعال  # شروع Paper Test
واچ             # دیدن نمادهای زیر نظر
قیف             # دیدن قیف کامل
```

⚠️ **ترید واقعی را روشن نکنید.** این نسخه برای Paper Test است.

---

## به‌روزرسانی: پاکسازی پنل و گزارش‌گیری

### حذف لیست ثابت ارزها
منطق `refresh_universe` (که ۳۰ کوین پرحجم را انتخاب می‌کرد) کامل حذف شد.
V3 **کل بازار** را اسکن می‌کند: هر قراردادی که پامپ ۲۴ ساعته‌اش به آستانه
برسد، خودکار وارد Watchlist می‌شود. دلیل: محدودکردن به ۳۰ کوین پرحجم باعث
می‌شد دقیقاً همان پامپ‌های شدیدی که هدف استراتژی‌اند (معمولاً در کوین‌های
کوچک‌تر) از دست بروند. فیلتر نقدینگی همچنان با `MIN_24H_QUOTE_VOLUME`
اعمال می‌شود.

### دستور «واچ» کامل شد
حالا نشان می‌دهد: تعداد کل، شمارهٔ ردیف، نام ارز، درصد پامپ فعلی، اوج
ثبت‌شده (اگر از آن برگشته باشد)، تعداد ترید، و وضعیت هر نماد.

### گزارش‌گیری تحقیقاتی (پاسخ به نیاز تحلیل بعد از چند روز)

**دستور «گزارش کامل»** — همهٔ آمار موردنیاز در تلگرام:

| بخش | شامل |
|---|---|
| قیف | کاندید → ستاپ → معامله |
| نتیجه | برد/باخت، نرخ برد، سود ناخالص و خالص، کارمزد، PF، انتظار هر معامله |
| ریسک | میانگین برد/باخت، بزرگ‌ترین برد/باخت، حداکثر افت سرمایه، مدت معامله |
| نوع ورود | تفکیک FAST_CASCADE در برابر NORMAL_REVERSAL |
| دلیل خروج | حد ضرر / برگشت ساختاری |
| تمرکز | سهم بهترین نماد و سه نماد برتر (با هشدار اگر > ۵۰٪) |

**دستور «خروجی»** — ساخت `v3_paper_trades.csv` شامل هر معامله با:
قیمت ورود/خروج، حد ضرر، دلیل خروج، MFE، مدت، و **همهٔ اعداد لحظهٔ سیگنال**
(سایهٔ بالا، شتاب، دامنه، حجم، ATR، فاصلهٔ استاپ).

این فایل دقیقاً همان چیزی است که برای تحلیل بیرونی لازم است — مثل کاری
که روی دادهٔ Binance انجام شد.

### داده کجا ذخیره می‌شود؟

همه‌چیز در `runtime.db` (SQLite) کنار خود ربات:

| جدول | محتوا |
|---|---|
| `cycles` | هر معامله: ورود، خروج، سود/ضرر، کارمزد، دلیل |
| `watchlist` | هر کاندید: زمان ورود، اوج پامپ، تعداد ستاپ و ترید |
| `signal_snapshots` | عکس کامل شرایط در لحظهٔ هر تریگر (ورود **و** رد) |
| `cooldowns` | تاریخچهٔ استراحت هر نماد |
| `events` | رویدادهای کامل شامل MFE و مدت هر معامله |

برای بکاپ گرفتن از داده‌های تحقیق کافی است `runtime.db` را کپی کنی.
